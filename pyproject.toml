[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kurum-yedekleme"
version = "1.0.0"
requires-python = ">=3.11"
description = "Kurum klasörlerini E:\\Yedekler altına ZIP olarak yedekler"

[tool.setuptools.packages.find]
where = ["src"]
